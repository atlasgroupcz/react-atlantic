import { Timeline } from '../src/components/Timeline';
import { toBeDefinedTest, mountTest } from './shared';

describe('shared Timeline', () => {
    toBeDefinedTest(Timeline);
    mountTest(Timeline);
    // mockPropsCheckTest(mockCollapseProps, );
});
