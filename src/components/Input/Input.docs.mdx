---
name: Input
menu: Components
---

import { Playground } from 'docz';
import { InputFloatingLabelView, InputDefaultView, useInputFloatingLabel, useInputChange, useInputClearable } from '.';
import { wrap } from '@atlasgroup/react-wrap';
import { Icon } from '../Icon';
import { WrapperHorizontal, WrapperVertical } from '../../gatsby-theme-docz/components/Wrapper';

export const Inputs = {
    Default: wrap(InputDefaultView, useInputChange),
    Clearable: wrap(InputDefaultView, useInputClearable),
    ClearableFlaotingLabel: wrap(InputFloatingLabelView, useInputClearable),
};

# Input

Alternative to basic HTML input

## Basic usage

<Playground>
    <InputDefaultView />
</Playground>

## Using different sizes

<Playground>
    <WrapperHorizontal>
        <InputDefaultView size="small" />
        <InputDefaultView size="medium" />
        <InputDefaultView size="large" />
    </WrapperHorizontal>
</Playground>

## Full width

<Playground>
    <InputDefaultView isFullWidth />
</Playground>

## Other props

<Playground>
    <InputDefaultView isRound />
</Playground>

## Floating label

<Playground>
    <WrapperHorizontal>
        <InputFloatingLabelView label={'Input label'} size="small" />
        <InputFloatingLabelView label={'Input label'} size="medium" />
        <InputFloatingLabelView label={'Input label'} size="large" />
    </WrapperHorizontal>
</Playground>

## Prefix & sufix

<Playground>
    <WrapperHorizontal>
        <InputDefaultView prefix={<Icon name={'close'} />} />
        <InputDefaultView suffix={<Icon name={'close'} />} />
    </WrapperHorizontal>
</Playground>

## Hooks variants

It's highly reccomended to use custom hooks instead default ones provided by this lib, for more info see [know bugs section](#known-bugs)

```tsx
import { InputDefaultView, useInputChange, useInputClearable } from '.';
import { wrap } from '@atlasgroup/react-wrap';

export const Inputs = {
    Default: wrap(InputDefaultView, useInputChange),
    Clearable: wrap(InputDefaultView, useInputClearable),
};
```

### useInputChange

<Playground>
    <Inputs.Default isDisabled />
    <Inputs.Default defaultValue="Super input" />
</Playground>

### useInputClearable

<Playground>
    <Inputs.Clearable isDisabled />
    <Inputs.Clearable defaultValue="Super input" />
</Playground>

## Known bugs

-   `useInputClearable` clears value on input click
-   hooks do not accept nor pass props that are not handled by them
-   `useInputFloatingLabel` wraps `useInputDefault` nothing else
-   `InputFloatingLabelView` label overlflows input value

<Playground>
    <Inputs.Clearable defaultValue="Value to be cleared" />
    <InputFloatingLabelView value="Value to be cleared" label={'text which overflows value'} />
</Playground>
