import { CollapseProviderProps } from '..';

export type CollapseContextState = CollapseProviderProps;
export type CollapseContextValue = {};

export const defaultCollapseContextState: CollapseContextState = {
    activeKey: 1,
};
