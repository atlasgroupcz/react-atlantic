---
name: Skeleton
menu: Components
---

import { Playground } from 'docz';
import { Skeleton } from '.';
import { WrapperVertical } from '../../gatsby-theme-docz/components/Wrapper';

# Skeleton

Element to use while the main component in loading

## Basic usage

<Playground>
    <Skeleton height={50} width={100} />
</Playground>

## Sizes

<Playground>
    <WrapperVertical>
        <Skeleton size="small" width={200} />
        <Skeleton size="medium" width={200} />
        <Skeleton size="large" width={200} />
    </WrapperVertical>
</Playground>

## Known bugs

-   `shape`, `size` and `bgColor` does not work
