import { StyledSelectInput } from './styles';
import { SelectInputIcon, SelectInputValue } from './components';

export const SelectInput = Object.assign(StyledSelectInput, {
    Icon: SelectInputIcon,
    Value: SelectInputValue,
});
