---
name: Select
menu: Components
---

import { Playground } from 'docz';
import { Select as SelectView, SelectMulti as SelectMultiView, useSelect, useSelectMulti } from '.';
import { wrap } from '@atlasgroup/react-wrap';
import { WrapperHorizontal } from '../../gatsby-theme-docz/components/Wrapper';

export const Select = wrap(SelectView, useSelect);
export const SelectMulti = wrap(SelectMultiView, useSelectMulti);

export const options = [
    { value: 'UK', label: 'England 🇬🇧' },
    { value: 'CZ', label: 'Czech Republic 🇨🇿' },
];

# Select

Is an alternative to the [`<select>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select) element.

## Basic usage

```tsx
import { Select, useSelect, styled } from '@atlasgroup/react-atlantic';
import { wrap } from '@atlasgroup/react-wrap';

// Optional
const ProjectSpecificStyledSelect = styled(Select)`
    // ... Project specific styles ...
`;

const ProjectSpecificSelect = wrap(ProjectSpecificStyledSelect, useSelect);
```

> `Select` must be wrapped with `useSelect` to make the element interactive. Using the component with a custom hook is encouraged.

> Same goes for `SelectMulti` that should be wrapped with `useSelectMulti`.

## Different types

### Default

<Playground>
    <Select options={options} />
</Playground>

### Multi

<Playground>
    <SelectMulti options={options} />
</Playground>

## Different sizes

<Playground>
    <WrapperHorizontal>
        <Select options={options} size="small" />
        <Select options={options} size="medium" />
        <Select options={options} size="large" />
    </WrapperHorizontal>
</Playground>

## FullWidth

<Playground>
    <Select options={options} isFullWidth />
</Playground>

## Known issues

-   [#1] - Unnecessary wrapping of the component in each project
-   [#2] - The component is not accessible via keyboard (might be useful for accessibility)
-   [#3] - Third party component is used
