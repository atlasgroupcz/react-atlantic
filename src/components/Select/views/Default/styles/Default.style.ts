import { styled } from '../../../../../styled';
import { SelectContainer, SelectInput, SelectList } from '../../../components';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as _ from 'styled-components/node_modules/@types/react';

export const StyledSelectDefaultContainer = styled(SelectContainer)``;

export const StyledSelectDefaultInput = styled(SelectInput)``;

export const StyledSelectDefaultInputValue = styled(SelectInput.Value)``;

export const StyledSelectDefaultInputIcon = styled(SelectInput.Icon)``;

export const StyledSelectDefaultList = styled(SelectList)``;

export const StyledSelectDefaultListItem = styled(SelectList.Item)``;
